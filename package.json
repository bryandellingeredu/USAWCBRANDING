{
  "name": "USAWCBRANDING",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "vite": "^2.0.0"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}